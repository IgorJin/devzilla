function match(regex, inputString) {
  let result = ['']

  const createInterval = (intervals = []) => {
    
  }

  if (regex.length === 1 && createInterval([97, 122], [65, 90]).includes(regex.charCodeAt(0)))

  return result
}

const serviceChar = ['$', '?', '*', '.', '^']

let idx = 0
function test(result, rightResult) {
  console.log(`TEST #${idx++}`)
  return result === rightResult ? console.log('CORRECT') : `Error on #${idx++}`
}

test(match(q, q), ['q'])